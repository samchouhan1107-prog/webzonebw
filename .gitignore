

#Ignore vscode AI rules
.github\instructions\codacy.instructions.md

node_modules/

# HTTPS dev certificates (never commit)
certs/

# ER Studio license persistence (never commit customer/license data)
data/
# Real environment config (never commit secrets)
.env
